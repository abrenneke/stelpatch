pm_null {
    spawn_chance = {
        base = 275
        modifier = {
            factor = 3
            num_modifiers > 2
        }

        modifier = {
            num_modifiers > 1
            factor = 3
        }

        modifier = {
            num_modifiers > 0
            factor = 3
        }

    }

    is_null = yes
}

